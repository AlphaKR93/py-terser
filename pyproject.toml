[project]
name = "py-terser"
version = "0.1.0"
authors = [
    { name = "Alpha", email = "dev@alpha93.kr" }
]
requires-python = ">=3.14"
dependencies = [
    "aiostream>=0.7.1",
    "anyio>=4.14.2",
]

[dependency-groups]
cli = [
    "pydantic>=2.13.4",
    "tqdm>=4.68.4",
]
dev = [
    "ruff>=0.15.22",
    "ty>=0.0.61",
]

[project.scripts]
terser = "python_minifier.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/terser", "src/python_minifier", "src/alpha93"]
